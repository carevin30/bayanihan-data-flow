import Layout from "@/components/Layout";
import HouseholdsModule from "@/components/HouseholdsModule";

const Households = () => {
  return (
    <Layout>
      <HouseholdsModule />
    </Layout>
  );
};

export default Households;